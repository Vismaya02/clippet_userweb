// this is the file where all the variables for comparison and fields are maintained so we don thardcord in every pages

import utilController from "../utils/Utilcontroller";

// if any change in variable required just change in this file
class Configuration {
  static itemType = {
    item: "item",
  };
}

export default Configuration;
